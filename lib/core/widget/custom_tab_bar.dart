import 'package:flutter/cupertino.dart';

/// TODO : Amr  MAKE CustomIcon using ThemeDATA
class CustomTapBar extends StatelessWidget {
  const CustomTapBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
